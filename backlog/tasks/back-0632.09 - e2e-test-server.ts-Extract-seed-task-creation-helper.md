---
id: BACK-0632.09
title: "e2e-test-server.ts: Extract seed task creation helper"
status: Done
assignee:
  - "@agent"
created_date: 2026-07-12 14:01
updated_date: 2026-07-12 15:29
completed_date: 2026-07-12 15:20
labels:
  - tech-debt
  - refactoring
  - testing
milestone: m-15
dependencies: []
modified_files:
  - scripts/e2e-test-server.ts
parent_task_id: BACK-0632
priority: low
ordinal: 432000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`scripts/e2e-test-server.ts` has 7 duplicate blocks (L32-155): Each task is created with the same `_core.createTask({...}, false)` pattern. 7 seed tasks (task-1 through task-7) each define `id`, `title`, `status`, `priority`, `assignee`, `labels`, `createdDate`, and optional fields, then pass through `as unknown as Task`.

Extract `function createSeedTask(partial: { id: string; title: string; status: string; priority: string; assignee?: string[]; labels?: string[]; createdDate?: string; description?: string })` that wraps `_core.createTask()` with the common `false` flag and `as unknown as Task` cast.

Files: `scripts/e2e-test-server.ts`
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 All seed task creations use a shared createSeedTask helper
- [x] #2 bun run check:types passes
- [x] #3 bun test passes
- [x] #4 bun run check . --write passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Add a `createSeedTask(core, partial)` helper to scripts/e2e-test-server.ts that wraps `core.createTask(partial, false)` with the `as unknown as Task` cast inline. Replace all 9 seed task blocks (task-1 through task-9, lines 32–140) with single-line calls. The helper absorbs the `as Task["status"]` / `as Task["priority"]` type assertions so callers just pass plain strings.

Steps:
1. Add `createSeedTask` function before the seed task blocks
2. Replace each `_core.createTask({...}, false)` block with `createSeedTask(_core, {...})`
3. Run `bun run check scripts/e2e-test-server.ts --write`
4. Run `bun run check:types`
5. Run `bun test`
6. Commit with message: `refactor(test): BACK-0632.09 - e2e-test-server.ts: Extract seed task helper`
<!-- SECTION:PLAN:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## e2e-test-server.ts: Extract seed task creation helper

- Extracted `createSeedTask(core, partial)` — replaces 9 seed task blocks
- Script went from 178→94 lines (-84 net)
- e2e-test-server.ts: 7→0 duplicate blocks ✅
- Commit: `5854ad77`
- Verification: `bun test` ✅
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 bunx tsc --noEmit passes when TypeScript touched
- [x] #2 bun run check . passes when formatting/linting touched
- [x] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->