---
id: BACK-0599.04
title: "backlog.ts: Draft-Operationen auslagern"
status: To Do
assignee: []
created_date: 2026-06-28 18:19
updated_date: 2026-07-08 19:14
labels:
  - refactoring
  - tech-debt
  - large-file
milestone: m-15
dependencies: []
parent_task_id: BACK-0599
priority: high
ordinal: 379000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Extrahiere Draft-Operationen aus backlog.ts in src/core/draft-operations.ts.

Betrifft (ca. 200 Zeilen):
- updateDraft (1792–1804) — Speichert Draft mit forced "Draft" Status + git
- updateDraftFromInput (1806–1826) — Wendet Input auf Draft an
- promoteDraftWithUpdates (1853–1904) — Draft→Task mit neuer ID (~52 Zeilen)
- demoteTaskWithUpdates (1906–1945) — Task→Draft mit neuer ID (~40 Zeilen)
- promoteDraft (2475–2485) — Simple file-move Variante
- demoteTask (2487–2497) — Simple file-move Variante
- archiveDraft (2463–2473) — Archiviert Draft

Abhängigkeiten: Nutzt applyTaskUpdateInput, formatDateStamp, withGitCommit, generateNextId (aus 0599.03/0599.02/0599.01).

API: Funktionen erhalten TaskOpDeps (wie 0599.03) + task-/draft-spezifische Parameter.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 src/core/draft-operations.ts existiert mit allen Draft-Methoden
- [ ] #2 promoteDraftWithUpdates + demoteTaskWithUpdates extrahiert und nutzen shared Dependencies
- [ ] #3 backlog.ts delegiert Draft-Operationen an draft-operations.ts
- [ ] #4 bun run check . --write passes
- [ ] #5 bun test --parallel passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Dateien
- Erstellen: src/core/draft-operations.ts
- Bearbeiten: src/core/backlog.ts

## Schritt 1: Extraktion

promoteDraftWithUpdates und demoteTaskWithUpdates teilen Struktur:
1. applyTaskUpdateInput aufrufen
2. withCreateLock + generateNextId (neue ID erzeugen)
3. Alte Datei löschen, neue schreiben
4. contentStore updaten
5. withGitCommit

```typescript
export interface DraftOpDeps {
  filesystem: FileSystem;
  contentStore?: ContentStore;
  git: GitOperations;
  idGenerator: { generateNextId(type: EntityType, parent?: string): Promise<string> };
  withCreateLock: <T>(fn: () => T) => T;
  applyTaskUpdateInput: (task: Task, input: TaskUpdateInput) => Promise<boolean>;
  shouldAutoCommit: (override?: boolean) => Promise<boolean>;
}
```

## Schritt 2: backlog.ts

Behält delegation:
```typescript
async promoteDraft(draftId, autoCommit?) {
  return draftOps.promoteDraft(draftOpDeps, draftId, autoCommit);
}
```

## Verifikation
```bash
bun run check . --write
bunx tsc --noEmit
bun test --parallel --timeout=20000 src/test/draft-*
```
<!-- SECTION:PLAN:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 bunx tsc --noEmit passes when TypeScript touched
- [ ] #2 bun run check . passes when formatting/linting touched
- [ ] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->