---
id: BACK-0632.08
title: "label.ts: Extract config loading helper + entity iteration utility"
status: Done
assignee:
  - "@agent"
created_date: 2026-07-12 14:01
updated_date: 2026-07-12 15:29
completed_date: 2026-07-12 15:12
labels:
  - tech-debt
  - refactoring
  - cli
milestone: m-15
dependencies: []
modified_files:
  - src/commands/label.ts
parent_task_id: BACK-0632
priority: medium
ordinal: 431000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`src/commands/label.ts` has 4 duplicate blocks:

1. **Command bootstrap boilerplate** (L66-118, L120-207, L209-231, L233-261, L263-291): All 5 label subcommands (list, add, rename, remove, set-color, remove-color) start with the same 5-line pattern:
```typescript
const cwd = await requireProjectRoot();
const core = new Core(cwd);
const config = await core.filesystem.loadConfig();
if (!config) { console.error(...); process.exit(EXIT.ERROR); }
```
Extract `ensureProjectConfig()` helper that returns `{core, config}` or exits.

2. **Label rename entity iteration** (L166-204): Tasks/docs/decisions loops for label rename are identical — each does `listXxx()`, `renameInEntity()`, then `try { await update } catch { warn }`. Extract `function updateLabelsOnEntities(core, oldName, newName)`.

Files: `src/commands/label.ts`
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 All label subcommands use a shared ensureProjectConfig() helper
- [x] #2 Label rename entity iteration (tasks/docs/decisions) uses shared utility
- [x] #3 bun run check:types passes
- [x] #4 bun test passes
- [x] #5 bun run check . --write passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Extract `ensureProjectConfig()` helper to replace 5-line bootstrap boilerplate in all 6 label subcommands (list, add, rename, remove, set-color, remove-color). Extract `updateLabelsOnEntities(core, oldName, newName)` to consolidate tasks/docs/decisions rename iteration from the rename handler (L166-204). Single file change to src/commands/label.ts. Verify with check:types + test.
<!-- SECTION:PLAN:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## label.ts: Extract config loading helper + entity iteration utility

- Extracted `ensureProjectConfig()` — replaces 6× repeated bootstrap boilerplate
- Extracted `updateLabelsOnEntities(core, oldName, newName)` — collapses 3× entity iteration loops
- Net: +66/-88 lines, label.ts: 4→2 duplicates
- Commit: `2dbb50dc`
- Verification: `bun test` ✅
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 bunx tsc --noEmit passes when TypeScript touched
- [x] #2 bun run check . passes when formatting/linting touched
- [x] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->