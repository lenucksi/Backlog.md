---
id: BACK-0632.06
title: "TaskDetailsModal.tsx: Extract shared milestone resolution utility"
status: Done
assignee:
  - "@agent"
created_date: 2026-07-12 14:01
updated_date: 2026-07-12 14:54
completed_date: 2026-07-12 14:54
labels:
  - tech-debt
  - refactoring
  - web-ui
milestone: m-15
dependencies: []
modified_files:
  - src/web/components/TaskDetailsModal.tsx
parent_task_id: BACK-0632
priority: medium
ordinal: 429000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`src/web/components/TaskDetailsModal.tsx` has 4 duplicate blocks:

1. **resolveMilestoneToId / resolveMilestoneLabel** (L102-183, L184-229+): Two 60+ line `useCallback` functions that are ~90% identical — both normalize input, build alias keys, define `idMatchesAlias`/`findIdMatch`, search active then archived milestones. Only the final return value differs (id vs label). Extract a shared `resolveMilestone` utility that returns a result with both `id` and `label`.

2. **Demote/Archive sidebar buttons** (L1322-1362): Two sections with identical `div.border.rounded-lg > button.w-full` wrapper structure, only icon/color/text differs. Extract `<SidebarActionButton>` component.

3. **Archived/Cross-branch indicator banners** (L730-748, L750-769): Two warning banners with identical layout (flex, icon, text). Only color/style class and icon SVG differ. Extract `<InfoBanner variant icon>` component.

Files: `src/web/components/TaskDetailsModal.tsx`
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 resolveMilestoneToId and resolveMilestoneLabel share a common utility
- [x] #2 Demote/Archive sidebar buttons use a shared component
- [x] #3 Warning banners (archived/cross-branch) use a shared component
- [x] #4 bun run check:types passes
- [x] #5 bun test passes
- [x] #6 bun run check . --write passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Extract 3 shared utilities in TaskDetailsModal.tsx (same file, no new files):

1. **resolveMilestone()** — module-level pure function replacing the two ~90% identical `useCallback` hooks (L102-242). Returns `{ id, label }`. Both callbacks become thin `useCallback` wrappers.

2. **SidebarActionButton** — component for the duplicate Demote/Archive sidebar button blocks (L1322-1362). Uses variant prop (`"warning" | "danger"`) mapped to Tailwind class strings (avoiding dynamic class limitation).

3. **InfoBanner** — component for the duplicate Archived/Cross-branch indicator banners (L730-771). Uses `color` prop (`"red" | "amber"`) + `icon` + `children` for content divergence.

No new files, no icons.tsx changes, no behavior change.
<!-- SECTION:PLAN:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## TaskDetailsModal.tsx: Extract shared milestone resolution utility

- **resolveMilestone()**: Combined 2× `useCallback` (60+ lines each) → module-level pure function, eliminating duplicate alias/ID resolution logic
- **SidebarActionButton**: Extracted `variant`-based component for Demote/Archive buttons
- **InfoBanner**: Extracted `color`-based component for archived/cross-branch banners
- **Net**: +171/-194 lines, TaskDetailsModal.tsx: 4→3 duplicates
- **Commit**: `7256b981`
- **Verification**: `bun test` ✅, `check:types` ✅, `check --write` ✅
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 bunx tsc --noEmit passes when TypeScript touched
- [x] #2 bun run check . passes when formatting/linting touched
- [x] #3 bun test (or scoped test) passes
- [x] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->