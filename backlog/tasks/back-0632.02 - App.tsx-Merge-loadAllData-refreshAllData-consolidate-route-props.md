---
id: BACK-0632.02
title: "App.tsx: Merge loadAllData/refreshAllData + consolidate route props"
status: Done
assignee: []
created_date: 2026-07-12 14:01
updated_date: 2026-07-12 14:29
completed_date: 2026-07-12 14:29
labels:
  - tech-debt
  - refactoring
  - web-ui
milestone: m-15
dependencies: []
modified_files:
  - src/web/App.tsx
parent_task_id: BACK-0632
priority: high
ordinal: 425000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`src/web/App.tsx` has 10 duplicate blocks from two patterns:

1. **loadAllData vs refreshAllData** (L286-329, L331-374): Two 19-line functions that are nearly identical — both do the same `Promise.all` with 8 API calls, the same setters, same `archivedKeys`/`milestoneAliases`/`applySearchResults` logic. Only difference: error message text and no `finally` in `refreshAllData`. Extract a shared `fetchAllData()` that returns the data, call from both.

2. **Route element props** (L616-730+): Route elements for `index`, `board/:id/:title`, `tasks`, `tasks/:id`, `tasks/:id/:title`, `stats`, `settings`, `docs`, `docs/:id` all pass 10-15 identical props (`tasks`, `statuses`, `milestones`, `availableLabels`, `labelColors`, `authorColors`, etc.) to their page components. Create a `<RoutePage>` wrapper or consolidate via context to eliminate prop drilling repetition.

Files: `src/web/App.tsx`
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 loadAllData and refreshAllData share a common fetchAllData() helper (only error/cleanup differs)
- [x] #2 Route elements no longer repeat the same 10+ props per route — use a wrapper or context
- [x] #3 bun run check:types passes
- [x] #4 bun test passes
- [x] #5 bun run check . --write passes
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## App.tsx: Merge loadAllData/refreshAllData + consolidate route props

### What was done
Eliminated all 10 duplicate-block warnings in App.tsx (10→0):
1. **loadAllData/refreshAllData merge**: Extracted shared `fetchAllData(isFullLoad)` — both callbacks now delegate to it
2. **Route props consolidation**: Created JSX element variables (`boardPageElement`, `taskListElement`, `milestonesPageElement`) assigned once before return statement, then referenced from routes

### Files changed
- `src/web/App.tsx` (modified, net -110 lines)

### Verification
- `bun test` → exit 0
- `bun run check src/web/ --write` → clean

### Commit
`43cc57eb` on branch `tasks/back-0632.02-app-refactor`
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 bunx tsc --noEmit passes when TypeScript touched
- [ ] #2 bun run check . passes when formatting/linting touched
- [ ] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->