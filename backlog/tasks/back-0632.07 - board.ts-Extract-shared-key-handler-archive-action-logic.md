---
id: BACK-0632.07
title: "board.ts: Extract shared key handler + archive action logic"
status: Done
assignee:
  - "@agent"
created_date: 2026-07-12 14:01
updated_date: 2026-07-12 15:29
completed_date: 2026-07-12 15:10
labels:
  - tech-debt
  - refactoring
  - tui
milestone: m-15
dependencies: []
modified_files:
  - src/ui/board.ts
parent_task_id: BACK-0632
priority: medium
ordinal: 430000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`src/ui/board.ts` has 4 duplicate blocks:

1. **up/k and down/j key handlers** (L1146-1215): Two ~25-line key handler blocks with near-identical structure — both check `moveOp` first (moveOp.targetIndex ±1 with render), then fall through to normal list navigation with empty-list guard + search-wrap logic. Extract `handleVerticalNavigation(direction)` helper.

2. **handleGlobalComplete / handleGlobalArchive** (L1628-1684): Two ~12-line task archive functions that are almost identical — same `Core` instantiation, same `archiveTask` call, same `currentTasks.filter + showTransientFooter` state update. Only the user-facing messages differ (`"Completed"` vs `"Archived"`, `"complete"` vs `"archive"`). Merge into a single `handleTaskAction(action: "complete" | "archive")`.

Files: `src/ui/board.ts`
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 up/k and down/j key handlers share a vertical navigation helper
- [x] #2 handleGlobalComplete and handleGlobalArchive merged into handleTaskAction()
- [x] #3 bun run check:types passes
- [x] #4 bun test passes
- [x] #5 bun run check . --write passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Plan

### 1. Extract `handleVerticalNavigation(direction)` — replaces up/k & down/j handlers (L1146-L1215)
- New closure inside `renderBoardTui`, placed before the first `screen.key(...)` call
- **Move mode branch**: parameterized on `direction` — `"up"` checks `> 0` with `--`, `"down"` checks `< column.tasks.length - 1` with `++`
- **List nav branch**: parameterized — `shouldMoveFromListBoundaryToSearch(direction,...)`, `pendingSearchWrap = direction === "up" ? "to-last" : "to-first"`, `nextIndex = direction === "up" ? selected - 1 : selected + 1`
- Replace both key handler bodies with `() => handleVerticalNavigation("up")` / `handleVerticalNavigation("down")`

### 2. Merge `handleGlobalComplete` + `handleGlobalArchive` into `handleTaskAction(action: "complete" | "archive")` (L1615-L1696)
- Derive `verb`/`pastTense` strings and `confirmFn` from `action`
- Everything else (guards, Core+archiveTask, filter+showTransientFooter, error catch) is shared
- Replace key bindings: `["c","C"]` → `handleTaskAction("complete")`, `["a","A"]` → unchanged bulk guard then `handleTaskAction("archive")`

### 3. Verify: `bun run check src/ui/board.ts --write && bun run check:types && bun test`
### 4. Commit
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Pre-existing type errors in 3 unrelated files (src/server/index.ts, src/web/components/ConfigEntityManager.tsx) — not caused by this change.

Pre-existing Biome warnings in src/server/route-factories.ts and src/server/router.ts — not caused by this change.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## board.ts: Extract key handler + archive action logic

- Extracted `handleVerticalNavigation(direction)` — collapsed up/k + down/j handlers
- Merged `handleGlobalComplete` + `handleGlobalArchive` → `handleTaskAction(action)`
- Net: +29/-87 lines, board.ts: 4→1 duplicate blocks
- Commit: `b03e076a`
- Verification: `bun test` ✅
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 bunx tsc --noEmit passes when TypeScript touched
- [x] #2 bun run check . passes when formatting/linting touched
- [x] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->