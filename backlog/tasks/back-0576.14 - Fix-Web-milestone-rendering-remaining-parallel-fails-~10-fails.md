---
id: BACK-0576.14
title: Fix Web milestone rendering + remaining parallel fails (~10 fails)
status: Done
assignee:
  - "@jo"
created_date: 2026-06-29 17:58
updated_date: 2026-07-05 11:19
completed_date: 2026-07-05 11:19
labels:
  - tests
  - parallel
  - bug
milestone: m-15
dependencies: []
parent_task_id: BACK-576
priority: high
ordinal: 328140
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Problem
Letzte Rests fails nach den Big-Impact-Fixes (576.09-576.13). Enthält die Rest-Kategorien:
- Web milestones JSDOM (2 fails)
- buildRemoteTaskIndex / parallel-remote-loading (4 fails) 
- status-callback / id-generation (2 fails — falls core.fs-Fix nicht reicht)
- cross-modality parity (1 fail — falls 576.13 nicht reicht)

## Voraussetzung
BACK-576.09, 576.10, 576.11, 576.12, 576.13 müssen VOR diesem Ticket erledigt sein.

## Vorgehen
1. **Vollen Suite unter --parallel laufen lassen**:
```bash
bun test --parallel --timeout=10000 --path-ignore-patterns='worktrees/*' --path-ignore-patterns='src/test/e2e/*' --path-ignore-patterns='src/test/tui-interactive-editor-handoff.test.ts' --path-ignore-patterns='src/test/runtime-cwd.test.ts' 2>&1 | grep '(fail)' | sort -u
```

2. **Jeden Fail einzeln analysieren** — vermutlich bleiben nur noch:
   - `web-milestones-page-search.test.tsx` (JSDOM-Isolation)
   - `build-remote-task-index.test.ts` / ähnlich (git branch mock state)
   - `status-callback.test.ts` (falls callback-state nicht isoliert)

3. **Web milestones fixen** — JSDOM global isolation:
```typescript
// In web-milestones-page-search.test.tsx: beforeEach/afterEach
let dom: JSDOM;
let root: Root;

beforeEach(() => {
  dom = new JSDOM("<!DOCTYPE html><div id='root'></div>");
  global.document = dom.window.document;
  global.window = dom.window as unknown as Window & typeof globalThis;
});

afterEach(() => {
  // unmount + cleanup
  dom.window.close();
});
```

4. **buildRemoteTaskIndex fixen** — beforeEach mock isolation (wie 576.12 Bug C)

5. **Alle anderen** — jeden Fail einzeln anschauen: ist es ein State-Leak, ein Port-Konflikt, oder eine echte Race-Condition?

## End-Verifikation
```bash
bun test --parallel --timeout=10000 --path-ignore-patterns='worktrees/*' --path-ignore-patterns='src/test/e2e/*' --path-ignore-patterns='src/test/tui-interactive-editor-handoff.test.ts' --path-ignore-patterns='src/test/runtime-cwd.test.ts' 2>&1 | tail -8
# Erwartet: 0 fail (ausser bekannte pre-existing bugs)
bun run check .
```
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Voller Suite unter --parallel hat 0 fails (ausser bekannte pre-existing BUGS die nichts mit --parallel zu tun haben)
- [x] #2 Suite läuft unter 120s
- [x] #3 bun run check . pass
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
**BACK-576.14 — Fix Web milestone rendering + remaining parallel fails**

## Result
Zero remaining failures under `--parallel`. All 3 acceptance criteria met:

- AC #1 ✅: `bun run test:fails` = 0 failures (1910 pass, 17 skip)
- AC #2 ✅: Suite runs in 72.75s (well under 120s limit)
- AC #3 ✅: `bun run check .` passes (382 files, no fixes)

## What happened
All previous sub-tickets (576.09–576.13) had already resolved the ~10+ remaining parallel failures. No additional changes were needed — this task verified the entire pipeline works clean.

## Verification chain
1. `bun run test:fails` — 0 failures under --parallel
2. `bun run test` — 1910 pass, 0 fail (85.90s serial)
3. `bun run check .` — clean
4. `bun run build` — dist/backlog built successfully
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 bunx tsc --noEmit passes when TypeScript touched
- [ ] #2 bun run check . passes when formatting/linting touched
- [ ] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->