---
id: BACK-0599.09
title: "backlog.ts: AC-Operations auslagern"
status: To Do
assignee: []
created_date: 2026-07-08 19:13
updated_date: 2026-07-08 19:16
labels:
  - refactoring
  - tech-debt
  - large-file
milestone: m-15
dependencies: []
parent_task_id: BACK-0599
priority: high
ordinal: 379000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Extrahiere Acceptance-Criteria- und Definition-of-Done-Operationen aus backlog.ts in src/core/ac-operations.ts.

Betrifft:
- Core-Methoden: addAcceptanceCriteria (2502–2520), removeAcceptanceCriteria (2526–2558), checkAcceptanceCriteria (2565–2602), listAcceptanceCriteria (2607–2614)
- Module-level: resolveAcceptanceCriteriaFromInput (446–527), resolveDefinitionOfDoneFromInput (529–593)

Die 3 AC-Mutation-Methoden folgen identischem load-edit-save-Muster. Die 2 module-level Resolver sind ~70% strukturell identisch.

API: Mutation-Methoden erhalten filesystem + updateTask-Callback. Resolver sind pure functions (task, input) → boolean.

Dedup-Potential: Generic criteria resolver für AC + DoD (70% ähnlich, ~100 Zeilen Einsparung). In der Extraktion konsolidieren.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 src/core/ac-operations.ts existiert mit AC + DoD Mutation-Methoden
- [ ] #2 resolveAcceptanceCriteriaFromInput + resolveDefinitionOfDoneFromInput als generic criteria resolver konsolidiert
- [ ] #3 0599.01 EXTRAHIERT NICHT resolveAC/resolveDoD — bleiben für 0599.09
- [ ] #4 add/remove/check/listAcceptanceCriteria delegieren via filesystem + updateTask-Callback
- [ ] #5 backlog.ts importiert aus ac-operations.ts und delegiert
- [ ] #6 bun run check . --write passes
- [ ] #7 bun test --parallel passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Dateien
- Erstellen: src/core/ac-operations.ts
- Bearbeiten: src/core/backlog.ts

## Schritt 1: Generic criteria resolver

resolveAcceptanceCriteriaFromInput (446–527) und resolveDefinitionOfDoneFromInput (529–593) sind ~70% identisch:
- Gleiches rebuildIndices-Muster
- Gleiche add/check/uncheck/remove Struktur
- Unterschied: Feldname (acceptanceCriteriaItems vs definitionOfDoneItems), Index-Feldname (index vs index, gleich)

```typescript
interface CriteriaItem { text: string; checked?: boolean; index?: number; }
interface CriteriaFieldResolverOpts {
  fieldName: "acceptanceCriteriaItems" | "definitionOfDoneItems";
}
```

AC-Input enthält zusätzlich replace + check/uncheck — DoD nur add/remove/check/uncheck. Die Unterschiede als Option-Parameter abbilden.

## Schritt 2: Mutation-Methoden extrahieren

addAcceptanceCriteria, removeAcceptanceCriteria, checkAcceptanceCriteria, listAcceptanceCriteria (2502–2614):
- Identisches load-edit-save-Muster
- loadTask → mutate → updateTask

```typescript
export interface AcOpDeps {
  filesystem: FileSystem;
  updateTask: (task: Task, autoCommit?: boolean) => Promise<void>;
}
```

## Schritt 3: backlog.ts

Behält delegation. AC-Methoden waren vorher 4 × load-edit-save → jetzt ein generischer Helper + 4 thin delegations.

## Verifikation
```bash
bun run check . --write
bunx tsc --noEmit
bun test --parallel --timeout=20000
```
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
- WICHTIG: resolveAcceptanceCriteriaFromInput (446–527) und resolveDefinitionOfDoneFromInput (529–593) sind module-level Functions in backlog.ts. Sie werden in 0599.01 (Task-Input-Resolver) extrahiert oder bleiben bis 0599.09 drin. Lösung: 0599.01 lässt sie liegen (nicht in task-input-resolvers.ts), 0599.09 extrahiert sie direkt als Teil von ac-operations.ts mit generic resolver.
- Die load-edit-save-Muster in add/remove/check/list sind 4× identisch: loadTask → Array-Mutation → updateTask. Das wird im generic criteria resolver konsolidiert.
- Der generic resolver ist das dedup-highlight (~100 Zeilen Einsparung, AC + DoD 70% Overlap).
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 bunx tsc --noEmit passes when TypeScript touched
- [ ] #2 bun run check . passes when formatting/linting touched
- [ ] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->