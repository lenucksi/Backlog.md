---
id: BACK-0599.07
title: "backlog.ts: Facade — Rest auf ≤800 Zeilen komprimieren"
status: To Do
assignee: []
created_date: 2026-06-28 18:19
updated_date: 2026-07-08 19:14
labels:
  - refactoring
  - tech-debt
  - large-file
milestone: m-15
dependencies: []
parent_task_id: BACK-0599
priority: high
ordinal: 382000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Letzter Subtask — backlog.ts von ~1.000+ auf ≤800 Zeilen reduzieren.

Nach Extraktion aller 10 vorherigen Subtasks (0599.01–.06 + .08–.11) bleiben in backlog.ts:

**Bleibt im Core (ca. 500 Zeilen):**
- Konstruktor + Properties + Lifecycle (685–734, ~50 Zeilen)
- Config: config getter, ensureConfigLoaded, shouldAutoCommit, getGitOps, getBacklogDirectoryName, reinitializeProjectRoot (~60 Zeilen)
- Delegations-Methoden: Thin wrapper die an extrahierte Module delegieren (~60 Zeilen, 1 Zeile pro Methode)
- Services: getContentStore, getSearchService, disposeSearchService, disposeContentStore, filesystem getter, gitOps getter (~30 Zeilen)
- TUI Editor: editTaskInTui, openEditor (~112 Zeilen) — bleibt hier wegen blessed screen dependency
- ensureConfigMigrated (~23 Zeilen) — delegiert an config-migration.ts
- Module-level: normalizeDocumentTypeInput (vergessen in .01)

**Entfernt in Extraktion:**
- getDocument, getDocumentContent → wandern zu 0599.08

**Ziel:** backlog.ts von 3.138 auf ≤800 Zeilen. Das erfordert auch Struktur-Vereinfachung:
- Imports konsolidieren (42 imports → ~15 nach Extraktion)
- this._filesystem durch this.filesystem getter ersetzen (wo sinnvoll)
- Methoden-Reihenfolge nach Domäne gruppieren (nicht historisch gewachsen)
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 backlog.ts ≤ 800 Zeilen
- [ ] #2 Alle 10 extrahierten Module werden importiert und delegieren
- [ ] #3 Imports von 42 auf ≤15 reduziert
- [ ] #4 Methoden-Reihenfolge nach Domäne gruppiert
- [ ] #5 bun run check . --write passes
- [ ] #6 bun test --parallel passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Dateien
- Bearbeiten: src/core/backlog.ts (massiv schrumpfen)

## Schritt 1: Aufräumen

Nachdem alle 10 Subtasks extrahiert haben:
- Entferne alle extrahierten Methoden (ca. 2.000 Zeilen)
- Ersetze durch delegation:
```typescript
async createTaskFromInput(input, autoCommit?) {
  return taskOps.createTaskFromInput(this._buildTaskOpDeps(), input, autoCommit);
}
```

## Schritt 2: Imports konsolidieren

42 imports reduzieren — viele Typ-Imports können zu einem shared type barrel zusammengefasst werden.
Bestehende Module (task-input-resolvers, task-operations, etc.) werden importiert.

## Schritt 3: Struktur

Methoden-Reihenfolge nach Domäne:
1. Constructor + lifecycle
2. Config
3. Services (getContentStore, getSearchService, etc.)
4. Delegation: Task CRUD
5. Delegation: Draft Ops
6. Delegation: Decision/Document
7. Delegation: Bulk/Sequences
8. Delegation: AC Ops
9. Delegation: Backlinks/Milestones
10. Delegation: Config Migration
11. TUI Editor (bleibt)
12. Statistics/Query Delegation

## Schritt 4: Verifikation

```bash
bun run check . --write
bunx tsc --noEmit
bun test --parallel --timeout=20000
```
<!-- SECTION:PLAN:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 bunx tsc --noEmit passes when TypeScript touched
- [ ] #2 bun run check . passes when formatting/linting touched
- [ ] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->