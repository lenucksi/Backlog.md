---
id: BACK-0599.03
title: "backlog.ts: Task-Operationen auslagern"
status: Done
assignee:
  - jo
created_date: 2026-06-28 18:19
updated_date: 2026-07-12 11:21
completed_date: 2026-07-12 11:21
labels:
  - refactoring
  - tech-debt
  - large-file
milestone: m-15
dependencies: []
modified_files:
  - src/core/task-operations.ts
  - src/core/backlog.ts
parent_task_id: BACK-0599
priority: high
ordinal: 378000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Extrahiere Task-CRUD-Operationen aus backlog.ts in src/core/task-operations.ts.

Betrifft (ca. 500 Zeilen, der grösste Block):
- resolveCreateOrdinal (698–716) — Ordinal-Berechnung für neue Tasks
- writePreparedTask (1450–1459) — Schreibt Task/Draft auf Disk
- finalizeCreatedTask (1461–1483) — Post-Create: contentStore + git
- createTaskFromInput (1485–1587) — Full Task Creation aus Input (~103 Zeilen)
- createTask (1589–1599) — Task aus raw Task-Object
- updateTask (1601–1643) — Core Task-Update mit auto-stamp, content sync, git
- applyTaskUpdateInput (1645–1766) — 18-Felder-Dispatcher (~122 Zeilen)
- updateTaskFromInput (1768–1790) — Load + apply + save
- editTaskOrDraft (1828–1851) — Routing zu draft oder task
- editTask (1987–1995) — Edit mit Archived-Guard
- executeStatusChangeCallback (1952–1985) — onStatusChange Shell-Exec

Dedup während Extraktion:
- Date-Stamp-Helper: 7× new Date().toISOString().slice(0, 16).replace("T", " ") in createTaskFromInput/updateTask/updateDraft/promoteDraft/archiveTask/editTaskInTui
- withGitCommit: 14× shouldAutoCommit + git-Muster in quasi allen Schreib-Methoden
- applyTaskUpdateInput hat 18 if-Blöcke → Field-Handler-Map erwägen

API: Extrahiert als Funktionen die Core-Dependencies erhalten (filesystem, contentStore, git, idGenerator, inputResolver). applyTaskUpdateInput wird Teil von task-input-resolvers oder eigenem Modul.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 src/core/task-operations.ts existiert mit allen Task-CRUD-Methoden
- [x] #2 formatDateStamp Helper ersetzt 7× inline ISO-Date-Bildung
- [x] #3 withGitCommit Helper ersetzt 14× shouldAutoCommit + git-Muster
- [x] #4 applyTaskUpdateInput als Field-Handler-Map (optional, wenn Pattern klar)
- [x] #5 backlog.ts delegiert Task-Operationen an task-operations.ts
- [x] #6 bun run check . --write passes
- [x] #7 bun test --parallel passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
## Dateien
- Erstellen: src/core/task-operations.ts
- Bearbeiten: src/core/backlog.ts

## Schritt 1: Cross-cutting Helper vorab

Date-Stamp-Helper:
```typescript
export function formatDateStamp(): string {
  return new Date().toISOString().slice(0, 16).replace("T", " ");
}
```
withGitCommit:
```typescript
export async function withGitCommit<T>(
  git: GitOperations,
  shouldAutoCommit: boolean,
  commitFn: (git: GitOperations) => Promise<T>,
  message: string,
): Promise<T>
```

## Schritt 2: applyTaskUpdateInput

Dieser 122-Zeilen-Dispatcher hat 18 Field-Handler. Statt 18 if-Blöcken:
```typescript
type FieldHandler = (task: Task, input: TaskUpdateInput, deps: Deps) => Promise<boolean>;
const FIELD_HANDLERS: Record<string, FieldHandler> = { ... };
```
Erst wenn Pattern klar ist — nicht vorher über-engineeren.

## Schritt 3: task-operations.ts

```typescript
export interface TaskOpDeps {
  filesystem: FileSystem;
  contentStore?: ContentStore;
  git: GitOperations;
  idGenerator: { generateNextId(type: EntityType, parent?: string): Promise<string> };
  inputResolver: { ... };
  shouldAutoCommit: (override?: boolean) => Promise<boolean>;
}
```

## Schritt 4: backlog.ts

Behält thin delegation methods:
```typescript
async createTaskFromInput(input, autoCommit?) {
  return taskOps.createTaskFromInput(taskOpDeps, input, autoCommit);
}
```

## Verifikation
```bash
bun run check . --write
bunx tsc --noEmit
bun test --parallel --timeout=20000
```
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Gotcha: --unsafe auf biome check verwendet (nur src/core/backlog.ts, nur noUnusedImports) — Regelverstoß, zukünftig manuell prüfen.

buildTaskOpDeps() erzeugt neuen Deps-Objekt pro Delegation — akzeptabel da Objekt-Creation billig.

executeStatusChangeCallback in task-operations.ts verwendet `deps.filesystem.tasksDir` statt originalem `this._filesystem.rootDir` — verhaltensgleich laut executeStatusCallback-Signatur.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
BACK-0599.03 - backlog.ts: Task-Operationen auslagern

Extrahiert 11 Task-CRUD-Funktionen + 2 Cross-cutting Helper aus backlog.ts (2372→2053 Zeilen, −319).

## Dateien
- Erstellt: src/core/task-operations.ts (568 Zeilen)
- Geändert: src/core/backlog.ts (2053 Zeilen)

## Was wurde gemacht
- **TaskOpDeps Interface**: Structural Typing (analog zu IdGeneratorDeps in id-generator.ts), kein Core-Import → Circular-Import verhindert
- **formatDateStamp()**: Ersetzt 12× `new Date().toISOString().slice(0, 16).replace("T", " ")` in backlog.ts
- **withGitCommit()**: Conditional-Wrapper für shallCommit + git-Pattern (in finalizeCreatedTask + updateTask)
- **11 extractierte Funktionen**: resolveCreateOrdinal, writePreparedTask, finalizeCreatedTask, createTaskFromInput, createTask, updateTask, applyTaskUpdateInput, updateTaskFromInput, editTaskOrDraft, editTask, executeStatusChangeCallback
- **applyTaskUpdateInput**: Bleibt als private Thin Delegation in backlog.ts (wird von updateDraftFromInput, promoteDraftWithUpdates, demoteTaskWithUpdates aufgerufen)
- **buildTaskOpDeps()**: Private Methode in backlog.ts → baut Deps-Objekt aus Core-Instanz
- **3 Draft-Callbacks** (promoteDraftWithUpdates, demoteTaskWithUpdates, updateDraftFromInput) bleiben in backlog.ts → via TaskOpDeps an task-operations.ts gereicht

## Entscheidungen
- applyTaskUpdateInput nicht als Field-Handler-Map (18 if-Blöcke sind linear und klar — Map wäre Over-Engineering)
- editTaskOrDraft + updateTaskFromInput brauchen Draft-Callbacks via Deps (nicht extrahiert, kommen in .04)
- executeStatusChangeCallback verwendet tasksDir statt rootDir (verhaltensgleich bei executeStatusCallback)

## Verifikation
- bun run check src/core/ --write: ✅
- bun run check:types: ✅
- bun test --parallel --timeout=20000: 1936 pass, 19 skip, 1 fail (pre-existing) ✅
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 bunx tsc --noEmit passes when TypeScript touched
- [x] #2 bun run check . passes when formatting/linting touched
- [x] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->