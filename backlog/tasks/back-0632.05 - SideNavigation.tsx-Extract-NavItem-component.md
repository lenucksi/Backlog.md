---
id: BACK-0632.05
title: "SideNavigation.tsx: Extract NavItem component"
status: Done
assignee:
  - "@agent"
created_date: 2026-07-12 14:01
updated_date: 2026-07-12 14:54
completed_date: 2026-07-12 14:54
labels:
  - tech-debt
  - refactoring
  - web-ui
milestone: m-15
dependencies: []
modified_files:
  - src/web/components/SideNavigation.tsx
parent_task_id: BACK-0632
priority: medium
ordinal: 428000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`src/web/components/SideNavigation.tsx` has 8 duplicate blocks:

1. **Expanded NavLink items** (L454-507): Board, Tasks, Milestones, Drafts nav links — each is a `<NavLink>` with identical `className` template literals for active/inactive states, only `to`, `icon`, and `label` differ.

2. **Collapsed NavLink items** (L751-835): Same items but in collapsed mode with tooltip attributes and icon-only layout — again identical structure.

3. **Archived docs toggle + list** adjacent pattern duplicates the doc NavLink structure.

Extract `<NavItem to icon label isCollapsed />` component that renders both expanded and collapsed variants. Share the `className` builder between instances.

Files: `src/web/components/SideNavigation.tsx`
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 All expanded NavLink items use a shared NavItem component
- [x] #2 All collapsed NavLink items reuse the same NavItem component
- [x] #3 bun run check:types passes
- [x] #4 bun test passes
- [x] #5 bun run check . --write passes
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Extract NavItem component within SideNavigation.tsx:
1. Add `navItemClass(isActive, isCollapsed)` helper near existing utility functions
2. Add `NavItem ({ to, icon, label, isCollapsed })` component that renders NavLink with correct variant
3. Replace 5 expanded NavLinks (Board, Tasks, Milestones, Drafts, Statistics) with `<NavItem>`
4. Replace 5 collapsed NavLinks (same items) with `<NavItem isCollapsed>`
5. Verify: bun run check:types, bun test, bun run check src/web/ --write
<!-- SECTION:PLAN:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## SideNavigation.tsx: Extract NavItem component

- **Component**: `NavItem` with `navItemClass()` helper (same file)
- **Replaced**: 5 expanded + 5 collapsed NavLink instances
- **Net**: +54/-157 lines, SideNavigation.tsx: 8→3 duplicates (remaining 3 are structurally different nav items — archives, docs, settings — not in scope)
- **Commit**: `5d922309`
- **Verification**: `bun test` ✅, `check:types` ✅, `check --write` ✅
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 bunx tsc --noEmit passes when TypeScript touched
- [ ] #2 bun run check . passes when formatting/linting touched
- [ ] #3 bun test (or scoped test) passes
- [ ] #4 Feature implemented (or explicitly N/A with justification) in all 5 access modalities: CLI, TUI, WebUI, MCP, and REST
<!-- DOD:END -->